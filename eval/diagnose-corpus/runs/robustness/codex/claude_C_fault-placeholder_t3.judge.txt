found=Y stage=N gold=unsubstituted-placeholder
why=It diagnosed the unsubstituted placeholder in run.cfg but treated the generated config line itself as the fault rather than the template-substitution step.
